# Build output
build/bin
/skill-manager

# Go
coverage.out

# Frontend build output (keep .gitkeep so //go:embed all:frontend/dist works in CI)
frontend/dist/*
!frontend/dist/.gitkeep

# Frontend misc
node_modules
frontend/package.json.md5

# Wails generated runtime (regenerado por `wails generate module` / `wails dev`)
frontend/wailsjs/runtime/
